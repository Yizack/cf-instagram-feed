{
  "name": "cf-instagram-feed",
  "description": "Javascript example to retrieve an Instagram profile feed using Cloudflare Workers and Instagram Basic Display API. Refresh token if cookie expired.",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "worker:dev": "tsc && wrangler dev",
    "worker:deploy": "tsc && wrangler deploy",
    "lint": "eslint --fix"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240701.0",
    "@stylistic/eslint-plugin": "^2.3.0",
    "@typescript-eslint/parser": "^7.16.0",
    "eslint": "^9.6.0",
    "itty-router": "^5.0.17",
    "ofetch": "^1.3.4",
    "typescript": "^5.5.3",
    "wrangler": "3.64.0"
  }
}
